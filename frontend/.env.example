# API 配置
VITE_API_BASE_URL=http://localhost:8080

# 加密配置
# 是否启用数据加密（true/false）
VITE_CRYPTO_ENABLED=true

# AES 加密密钥（32字节，生产环境必须修改）
VITE_CRYPTO_SECRET_KEY=GameLink2025SecretKey!@#123456

# AES 加密向量（16字节，生产环境必须修改）
VITE_CRYPTO_IV=GameLink2025IV!!!

# 注意：
# 1. 复制此文件为 .env.local（生产环境）
# 2. 复制此文件为 .env.development（开发环境）
# 3. 生产环境必须使用强密钥，不要使用默认值
# 4. 密钥和向量应该与后端保持一致
# 5. 不要将 .env.local 提交到 git
