# 📚 GameLink 业务状态字典

## 📋 文档说明

**文档类型**: 业务状态定义规范
**适用范围**: 前后端统一状态标准
**维护原则**: 单一数据源,前后端引用此文档

---

## 📦 订单状态 (Order Status)

### 状态定义

| 状态值 | 英文标识 | 中文名称 | 说明 | 可流转状态 |
|--------|----------|----------|------|-----------|
| `pending` | PENDING | 待支付 | 订单已创建,等待用户支付 | paid, cancelled |
| `paid` | PAID | 已支付 | 用户已完成支付,等待陪玩师确认 | confirmed, refunded |
| `confirmed` | CONFIRMED | 已确认 | 陪玩师已确认接单 | in_progress, cancelled |
| `in_progress` | IN_PROGRESS | 进行中 | 服务正在进行中 | completed |
| `completed` | COMPLETED | 已完成 | 服务已完成,等待评价 | - |
| `cancelled` | CANCELLED | 已取消 | 订单已取消 | - |
| `refunded` | REFUNDED | 已退款 | 订单已退款 | - |

### 后端实现 (Go)

```go
// internal/model/order.go
package model

type OrderStatus string

const (
    OrderStatusPending    OrderStatus = "pending"      // 待支付
    OrderStatusPaid       OrderStatus = "paid"         // 已支付
    OrderStatusConfirmed  OrderStatus = "confirmed"    // 已确认
    OrderStatusInProgress OrderStatus = "in_progress"  // 进行中
    OrderStatusCompleted  OrderStatus = "completed"    // 已完成
    OrderStatusCancelled  OrderStatus = "cancelled"    // 已取消
    OrderStatusRefunded   OrderStatus = "refunded"     // 已退款
)

// 状态流转规则
var OrderStatusTransitions = map[OrderStatus][]OrderStatus{
    OrderStatusPending:    {OrderStatusPaid, OrderStatusCancelled},
    OrderStatusPaid:       {OrderStatusConfirmed, OrderStatusRefunded},
    OrderStatusConfirmed:  {OrderStatusInProgress, OrderStatusCancelled},
    OrderStatusInProgress: {OrderStatusCompleted},
    OrderStatusCompleted:  {},
    OrderStatusCancelled:  {},
    OrderStatusRefunded:   {},
}

// 验证状态流转是否合法
func (s OrderStatus) CanTransitionTo(target OrderStatus) bool {
    allowedStates, exists := OrderStatusTransitions[s]
    if !exists {
        return false
    }
    for _, allowed := range allowedStates {
        if allowed == target {
            return true
        }
    }
    return false
}
```

### 前端实现 (TypeScript)

```typescript
// src/types/order.ts
export enum OrderStatus {
  PENDING = 'pending',           // 待支付
  PAID = 'paid',                 // 已支付
  CONFIRMED = 'confirmed',       // 已确认
  IN_PROGRESS = 'in_progress',   // 进行中
  COMPLETED = 'completed',       // 已完成
  CANCELLED = 'cancelled',       // 已取消
  REFUNDED = 'refunded'          // 已退款
}

// 状态显示文本映射
export const OrderStatusText: Record<OrderStatus, string> = {
  [OrderStatus.PENDING]: '待支付',
  [OrderStatus.PAID]: '已支付',
  [OrderStatus.CONFIRMED]: '已确认',
  [OrderStatus.IN_PROGRESS]: '进行中',
  [OrderStatus.COMPLETED]: '已完成',
  [OrderStatus.CANCELLED]: '已取消',
  [OrderStatus.REFUNDED]: '已退款',
};

// 状态流转规则
export const OrderStatusTransitions: Record<OrderStatus, OrderStatus[]> = {
  [OrderStatus.PENDING]: [OrderStatus.PAID, OrderStatus.CANCELLED],
  [OrderStatus.PAID]: [OrderStatus.CONFIRMED, OrderStatus.REFUNDED],
  [OrderStatus.CONFIRMED]: [OrderStatus.IN_PROGRESS, OrderStatus.CANCELLED],
  [OrderStatus.IN_PROGRESS]: [OrderStatus.COMPLETED],
  [OrderStatus.COMPLETED]: [],
  [OrderStatus.CANCELLED]: [],
  [OrderStatus.REFUNDED]: [],
};

// 验证状态流转
export function canTransitionTo(current: OrderStatus, target: OrderStatus): boolean {
  return OrderStatusTransitions[current]?.includes(target) ?? false;
}
```

---

## 💳 支付状态 (Payment Status)

### 状态定义

| 状态值 | 英文标识 | 中文名称 | 说明 |
|--------|----------|----------|------|
| `pending` | PENDING | 待支付 | 支付单已创建,等待支付 |
| `processing` | PROCESSING | 处理中 | 支付正在处理中 |
| `success` | SUCCESS | 支付成功 | 支付已成功完成 |
| `failed` | FAILED | 支付失败 | 支付失败 |
| `refunding` | REFUNDING | 退款中 | 退款申请处理中 |
| `refunded` | REFUNDED | 已退款 | 退款已完成 |
| `cancelled` | CANCELLED | 已取消 | 支付已取消 |

### 后端实现 (Go)

```go
// internal/model/payment.go
package model

type PaymentStatus string

const (
    PaymentStatusPending    PaymentStatus = "pending"      // 待支付
    PaymentStatusProcessing PaymentStatus = "processing"   // 处理中
    PaymentStatusSuccess    PaymentStatus = "success"      // 支付成功
    PaymentStatusFailed     PaymentStatus = "failed"       // 支付失败
    PaymentStatusRefunding  PaymentStatus = "refunding"    // 退款中
    PaymentStatusRefunded   PaymentStatus = "refunded"     // 已退款
    PaymentStatusCancelled  PaymentStatus = "cancelled"    // 已取消
)
```

### 前端实现 (TypeScript)

```typescript
// src/types/payment.ts
export enum PaymentStatus {
  PENDING = 'pending',           // 待支付
  PROCESSING = 'processing',     // 处理中
  SUCCESS = 'success',           // 支付成功
  FAILED = 'failed',             // 支付失败
  REFUNDING = 'refunding',       // 退款中
  REFUNDED = 'refunded',         // 已退款
  CANCELLED = 'cancelled'        // 已取消
}

export const PaymentStatusText: Record<PaymentStatus, string> = {
  [PaymentStatus.PENDING]: '待支付',
  [PaymentStatus.PROCESSING]: '处理中',
  [PaymentStatus.SUCCESS]: '支付成功',
  [PaymentStatus.FAILED]: '支付失败',
  [PaymentStatus.REFUNDING]: '退款中',
  [PaymentStatus.REFUNDED]: '已退款',
  [PaymentStatus.CANCELLED]: '已取消',
};
```

---

## 👤 用户状态 (User Status)

### 状态定义

| 状态值 | 英文标识 | 中文名称 | 说明 |
|--------|----------|----------|------|
| `active` | ACTIVE | 正常 | 账户正常可用 |
| `inactive` | INACTIVE | 未激活 | 账户未激活 |
| `banned` | BANNED | 已封禁 | 账户已被封禁 |
| `deleted` | DELETED | 已删除 | 账户已删除 |

### 实现

```go
// Go
type UserStatus string

const (
    UserStatusActive   UserStatus = "active"    // 正常
    UserStatusInactive UserStatus = "inactive"  // 未激活
    UserStatusBanned   UserStatus = "banned"    // 已封禁
    UserStatusDeleted  UserStatus = "deleted"   // 已删除
)
```

```typescript
// TypeScript
export enum UserStatus {
  ACTIVE = 'active',       // 正常
  INACTIVE = 'inactive',   // 未激活
  BANNED = 'banned',       // 已封禁
  DELETED = 'deleted'      // 已删除
}
```

---

## 🎮 陪玩师状态 (Player Status)

### 状态定义

| 状态值 | 英文标识 | 中文名称 | 说明 |
|--------|----------|----------|------|
| `pending` | PENDING | 待审核 | 申请已提交,等待审核 |
| `approved` | APPROVED | 已认证 | 审核通过,已认证 |
| `rejected` | REJECTED | 已拒绝 | 审核未通过 |
| `online` | ONLINE | 在线 | 当前在线可接单 |
| `offline` | OFFLINE | 离线 | 当前离线 |
| `busy` | BUSY | 忙碌 | 服务中,暂不接单 |
| `suspended` | SUSPENDED | 已暂停 | 账户已暂停服务 |

### 实现

```go
// Go
type PlayerStatus string

const (
    PlayerStatusPending   PlayerStatus = "pending"    // 待审核
    PlayerStatusApproved  PlayerStatus = "approved"   // 已认证
    PlayerStatusRejected  PlayerStatus = "rejected"   // 已拒绝
    PlayerStatusOnline    PlayerStatus = "online"     // 在线
    PlayerStatusOffline   PlayerStatus = "offline"    // 离线
    PlayerStatusBusy      PlayerStatus = "busy"       // 忙碌
    PlayerStatusSuspended PlayerStatus = "suspended"  // 已暂停
)
```

```typescript
// TypeScript
export enum PlayerStatus {
  PENDING = 'pending',       // 待审核
  APPROVED = 'approved',     // 已认证
  REJECTED = 'rejected',     // 已拒绝
  ONLINE = 'online',         // 在线
  OFFLINE = 'offline',       // 离线
  BUSY = 'busy',             // 忙碌
  SUSPENDED = 'suspended'    // 已暂停
}
```

---

## 💰 提现状态 (Withdrawal Status)

### 状态定义

| 状态值 | 英文标识 | 中文名称 | 说明 |
|--------|----------|----------|------|
| `pending` | PENDING | 待审核 | 提现申请已提交 |
| `approved` | APPROVED | 已批准 | 审核通过,处理中 |
| `processing` | PROCESSING | 处理中 | 正在转账处理 |
| `completed` | COMPLETED | 已完成 | 提现已到账 |
| `rejected` | REJECTED | 已拒绝 | 审核未通过 |
| `failed` | FAILED | 失败 | 转账失败 |

### 实现

```go
// Go
type WithdrawalStatus string

const (
    WithdrawalStatusPending    WithdrawalStatus = "pending"     // 待审核
    WithdrawalStatusApproved   WithdrawalStatus = "approved"    // 已批准
    WithdrawalStatusProcessing WithdrawalStatus = "processing"  // 处理中
    WithdrawalStatusCompleted  WithdrawalStatus = "completed"   // 已完成
    WithdrawalStatusRejected   WithdrawalStatus = "rejected"    // 已拒绝
    WithdrawalStatusFailed     WithdrawalStatus = "failed"      // 失败
)
```

```typescript
// TypeScript
export enum WithdrawalStatus {
  PENDING = 'pending',           // 待审核
  APPROVED = 'approved',         // 已批准
  PROCESSING = 'processing',     // 处理中
  COMPLETED = 'completed',       // 已完成
  REJECTED = 'rejected',         // 已拒绝
  FAILED = 'failed'              // 失败
}
```

---

## 🎁 优惠券状态 (Coupon Status)

### 状态定义

| 状态值 | 英文标识 | 中文名称 | 说明 |
|--------|----------|----------|------|
| `unused` | UNUSED | 未使用 | 优惠券可用 |
| `used` | USED | 已使用 | 优惠券已使用 |
| `expired` | EXPIRED | 已过期 | 优惠券已过期 |
| `invalid` | INVALID | 已失效 | 优惠券已失效 |

### 实现

```go
// Go
type CouponStatus string

const (
    CouponStatusUnused  CouponStatus = "unused"   // 未使用
    CouponStatusUsed    CouponStatus = "used"     // 已使用
    CouponStatusExpired CouponStatus = "expired"  // 已过期
    CouponStatusInvalid CouponStatus = "invalid"  // 已失效
)
```

```typescript
// TypeScript
export enum CouponStatus {
  UNUSED = 'unused',       // 未使用
  USED = 'used',           // 已使用
  EXPIRED = 'expired',     // 已过期
  INVALID = 'invalid'      // 已失效
}
```

---

## 📝 使用规范

### 1. 状态值规范
- 所有状态值使用 **小写英文 + 下划线** 格式 (snake_case)
- 前后端必须使用完全相同的状态值字符串
- 禁止在业务代码中硬编码状态字符串,必须使用常量/枚举

### 2. 状态流转规范
- 所有状态变更必须验证流转规则
- 不允许的状态流转应抛出明确错误
- 状态变更需记录操作日志

### 3. 新增状态规范
- 新增状态必须先在此文档中定义
- 同步更新前后端实现代码
- 更新相关业务逻辑和测试用例

### 4. 文档维护规范
- 状态定义变更需通知所有开发人员
- 保持前后端状态定义完全一致
- 定期审查状态使用情况

---

**文档版本**: v1.0.0
**创建时间**: 2025年1月10日
**最后更新**: 2025年1月10日
**维护团队**: GameLink开发团队
