# 开发环境配置文件
# 用于本地开发和测试

server:
  port: "8080"
  enable_swagger: true

database:
  type: "postgres"
  dsn: "host=localhost port=5432 user=gamelink password=gamelink dbname=gamelink_dev sslmode=disable"

cache:
  type: "memory"
  redis:
    addr: "127.0.0.1:6379"
    password: ""
    db: 0

crypto:
  enabled: false
  secret_key: "GameLink2025SecretKey!@#"
  iv: "GameLink2025IV!!!"
  methods:
    - POST
    - PUT
    - PATCH
  exclude_paths:
    - "/api/v1/health"
    - "/api/v1/ping"
    - "/api/v1/auth/refresh"
  use_signature: true

auth:
  jwt_secret: "MiBSQJJKEW2euVXKpvxRzjS1C5TCFlXx4RXGUXSdWpJ"
  token_ttl_hours: 24

seed:
  enabled: true

super_admin:
  email: "admin@gameLink.com"
  password: "123456"
  name: "Super Admin"
  phone: ""

admin_auth:
  mode: "jwt"
