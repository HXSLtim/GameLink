# 开发环境配置文件
# 用于本地开发和测试

server:
  port: "8080"
  enable_swagger: true

database:
  type: "postgres"
  dsn: "host=localhost port=5433 user=gamelink password=gamelink dbname=gamelink_dev sslmode=disable"

cache:
  type: "redis"
  redis:
    addr: "localhost:6380"
    password: ""
    db: 0

crypto:
  enabled: false
  # 开发环境使用固定密钥（生产环境必须通过环境变量配置）
  # 密钥长度要求：secret_key 必须 32 字节（AES-256），iv 必须 16 字节
  # 生成命令: openssl rand -base64 32 (secret) 或 openssl rand -base64 16 (iv)
  secret_key: "H/oguKMv23lWlivgq8snNZmTzSUp6KSHZnEEo1c0Ook="
  iv: "hTeObHJQ3nGDNs4H4O778A=="
  methods:
    - POST
    - PUT
    - PATCH
  exclude_paths:
    - "/api/v1/health"
    - "/api/v1/ping"
    - "/api/v1/auth/refresh"
  use_signature: true

auth:
  jwt_secret: "MiBSQJJKEW2euVXKpvxRzjS1C5TCFlXx4RXGUXSdWpJ"
  token_ttl_hours: 24

seed:
  enabled: true

super_admin:
  email: "admin@gameLink.com"
  # 开发环境强密码（16字节随机）：NNLeRYZN1IF3A/T80C7+Q6mU3xBZtdnu
  # 生产环境务必通过环境变量 SUPER_ADMIN_PASSWORD 配置不同的强密码
  # 生成命令: openssl rand -base64 24
  password: "NNLeRYZN1IF3A/T80C7+Q6mU3xBZtdnu"
  name: "Super Admin"
  phone: ""

admin_auth:
  mode: "jwt"

# 外部API凭证配置（可选）
external_apis:
  # 微信支付配置
  wechat_pay:
    app_id: ""                    # 微信支付应用ID
    mch_id: ""                    # 商户号
    api_key: ""                   # API密钥
    api_cert_path: ""             # API证书路径
    notify_url: ""                # 支付结果通知URL
    enabled: false                # 是否启用微信支付

  # 支付宝配置
  alipay:
    app_id: ""                    # 支付宝应用ID
    private_key_path: ""          # 应用私钥路径
    public_key_path: ""           # 支付宝公钥路径
    notify_url: ""                # 支付结果通知URL
    enabled: false                # 是否启用支付宝

  # 短信服务配置
  sms:
    provider: ""                  # 服务商: aliyun, tencent
    access_key: ""                # AccessKey ID
    secret_key: ""                # AccessKey Secret
    sign_name: ""                 # 短信签名
    enabled: false                # 是否启用短信服务

  # OSS对象存储配置
  oss:
    provider: ""                  # 服务商: aliyun, qcloud, minio
    endpoint: ""                  # 访问域名
    access_key: ""                # AccessKey ID
    secret_key: ""                # AccessKey Secret
    bucket: ""                    # 存储桶名称
    region: ""                    # 区域
    enabled: false                # 是否启用OSS
